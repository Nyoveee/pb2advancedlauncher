import React from 'react'

const AccountFooter = (props) => {
    return (
        <div id="accountFooter">        
            <p>To save an account, simply log in with that account once and it's credentials will be saved.</p>
        </div>
    )
}

export default AccountFooter